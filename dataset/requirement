
_get_random_vector -> [x,y,z]
_get_random_slice ->  slice  (valid -> treshole of slice is white)
_get_random_crop -> transformation
_add_to_dataset(row)
_save_slice(img_path=<id>_<allen|real>_<vetor>_<depth>_<crop>, img)

generate row until a count (1000)

Have a good cover with all angles and all depth 

The final csv should have these column : 
allen_path, real_path, every_spacial information to reconstruct the exact position including the random crop, regions (from the allen annotation).


---
The regions should serves to IOU? they should be included in the final image (rag) process. 

 